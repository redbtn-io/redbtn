/**
 * @file src/lib/chat-components/index.ts
 *
 * Public surface of the chat-components module. See `spec-schema.ts` for the
 * frozen `ChatComponentSpec` v1 schema and the rationale behind every invariant
 * (signed off in `~/assistant/chat-epic-4-interactive-signoff.md`).
 */

export * from './spec-schema';
